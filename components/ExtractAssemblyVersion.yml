  jobs:
  - job: ExtractAssemblyVersion
    steps:
    - checkout: self
      fetchDepth: 1
    - bash: |
       cat /etc/os-release | grep '^VERSION' /etc/os-release
      displayName: Extract Assembly Version