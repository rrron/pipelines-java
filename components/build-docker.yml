parameters:
  - name: paramn
    type: object
    default: 

# - name: dockerfilename

#     - "dockerfolder/Dockerfile"
#     - "dockerfolder/DockerfileHttpd"
# - name: personaltag
#   type: object
#   default:
#     - "$(Build.BuildId)"
#     - "$(Build.BuildNumber)"

steps:
  - ${{ each p in parameters.paramn }}:
    - script: 'echo ${{ p }}'
#   - task: Docker@2
#     inputs:
#       containerRegistry: 'DockerHub'
#       repository: 'nginx'
#       command: 'build'
#       Dockerfile: '${{ parameters.dockerfilename }}'
#       buildContext: ''
#       tags: '${{ parameters.personaltag }}'
#       addPipelineData: false
#       addBaseImageData: false